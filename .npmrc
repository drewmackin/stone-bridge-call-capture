# Stop `npm install` right away on an unsupported Node (package.json "engines").
# Node 24 has no prebuilt better-sqlite3, so npm would try to compile it and fail
# without Visual Studio. Node 20/22 install cleanly with no build tools (CI-verified).
engine-strict=true
